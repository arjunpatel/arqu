<!--
<%= form_for(@dictionary) do |f| %>
  <% if @dictionary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dictionary.errors.count, "error") %> prohibited this dictionary from being saved:</h2>

      <ul>
      <% @dictionary.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :word %><br />
    <%= f.text_field :word, :required => "required"%>
  </div>
  <div class="field">
    <%= f.label :voice_id %><br />
    <%= f.text_field :voice_id, :required => "required" %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, :size => "10x5" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
--> 